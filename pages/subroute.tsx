const Home = () => {
  return (
    <div>
      <h1>Example of a subroute</h1>
      <p>
        Basically you create new routes by creating new tsx files in the
        pages-folder. For instance if you create a pages/foo.tsx file, it will
        render the file with the route /foo
      </p>
    </div>
  );
};

export default Home;
